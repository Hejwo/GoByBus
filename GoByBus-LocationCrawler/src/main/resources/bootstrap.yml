spring.profiles.active: native

server:
  port: ${APP_PORT}

spring:
  application:
    name: GoByBus-LocationCrawler
  cloud:
    config:
        uri: ${CONFIG_SERVER_URI:http://localhost:8888}
        fail-fast: true