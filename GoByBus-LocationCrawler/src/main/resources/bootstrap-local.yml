# App settings
server.port: 8080
readPortionCron: '0/5 * * * * *'

# Kafka producer settings
kafka.newLocation:
  bootstrap.servers: localhost:9092
  topic: new_positions
  acks: 1
  retires: 0
  timeout: 5000